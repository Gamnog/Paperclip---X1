{
  "name": "firezard-pilot-pipeline",
  "version": "0.1.0",
  "description": "Firezard legal-monitoring digest pipeline (FIR-17 pilot MVP)",
  "main": "run.js",
  "scripts": {
    "start": "node run.js",
    "scheduler": "node scheduler.js",
    "test": "node test/classify.test.js && node test/nss-sbirka-scrape.test.js"
  },
  "dependencies": {
    "nodemailer": "^6.10.1"
  },
  "engines": {
    "node": ">=18"
  }
}
